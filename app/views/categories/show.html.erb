<h2><%= @category.name %></h2>

<ul>
<% @articles.each do |article| %>
  <li><%= link_to "#{article.title}", category_article_path(@category, article) %></li>
<% end %>
</ul>
<p><%= link_to 'New Article', new_category_article_path(@category) %></p>
